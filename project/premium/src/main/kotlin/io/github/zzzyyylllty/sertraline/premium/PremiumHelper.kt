package io.github.zzzyyylllty.sertraline.premium

/**
 * It seems you found this.
 * Correct, simply create a new empty project and then add a new class named io.github.zzzyyylllty.PremiumHelper
 * You can skip the Premium verification.
 * Of course, I could easily add the Premium version's features here, which would make it a bit more complicated for you.
 * But who cares?
 * Since this plugin is open-source anyway, you can do whatever you want. You've already found your way here,
 * so isn't it more convenient to build directly rather than creating a new Class? You know what I mean right, P-Chan?
 * Build artifacts is in `plugin/build/libs` Folder.
 *
 * 看起来你还是发现了这个。
 * 没错，只需要新建一个空的项目，然后新建一个 io.github.zzzyyylllty.PremiumHelper 的 Class
 * 你就可以跳过 Premium 校验了。
 * 当然，我完全可以把 Premium 版本的功能放到这里，你会麻烦一点。但管他呢。
 * 反正这个插件是开源的，你爱干不干吧。你都已经找到这里了，比起新建一个 Class，直接构建不是更为方便，你说对吧，阿P?
 *
 * - ChoTenChan
 */
class PremiumHelper {
}
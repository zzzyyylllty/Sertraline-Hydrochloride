FOOD_TEMPLATE:
  action:
    - trigger: onInteract
      require:
        - HAND
      type: KETHER
      async: true
      content: |
        command inline "effect give {{ sender }} {{ &effect }}" as console

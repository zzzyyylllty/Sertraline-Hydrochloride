FOOD_TEMPLATE:
  action:
    - trigger: onInteract
      require:
        - MAIN_HAND
      type: KETHER
      async: true
      content: |
        command inline "effect give {{ sender }} {{ &effect }}" as console
